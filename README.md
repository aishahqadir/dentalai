# DentalAI (v1)

Scaffolded minimal Next.js + TypeScript project with Supabase client placeholders.

Quick start:

```bash
npm install
npm run dev
```

Set environment variables from `.env.example` before running.
